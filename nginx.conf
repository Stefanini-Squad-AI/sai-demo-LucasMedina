server {
    listen 80;
    server_name localhost;

    # Servir los archivos estáticos
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

}